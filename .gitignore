# Dependencies (build-time only — devDependencies for `npm run build`)
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local env
.env
.env.local

# NOTE: ./build (precompiled JSX) and ./vendor (React) ARE committed on
# purpose so the site deploys with no build step and no CDN dependency.

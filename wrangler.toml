# Cloudflare Pages configuration (read automatically on deploy).
# This app is fully self-contained: React is vendored in ./vendor and the
# JSX is precompiled into ./build (both committed). No build step or CDN is
# required at deploy time — Pages serves the repository root as static assets.
name = "paragon-deal-os"
pages_build_output_dir = "."
